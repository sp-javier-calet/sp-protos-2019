using System;

namespace BehaviorDesigner.Runtime.Standalone
{
    public class Coroutine
    {
        public Coroutine()
        {
            throw new NotImplementedException(string.Format("{0} Not implemented in Standalone version", GetType()));
        }
    }
}

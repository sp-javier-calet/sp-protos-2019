#ifndef _SP_PROFILER_H_
#define _SP_PROFILER_H_

#if _MSC_VER
#define UNITY_WIN 1
#else
#define UNITY_OSX 1
#endif

#if UNITY_WIN
#define EXPORT_API __declspec(dllexport)
#else
#define EXPORT_API
#endif

extern "C"
{
    struct SPUnityProfilerGarbageInfo
    {
    	uint32_t heapMem;
        uint32_t usedMem;
        uint32_t maxCollectionsNum;
    	float stopWorldTime;
    	float markTime;
    	float reclaimTime;
    	float startWorldTime;
    	float collectionTime;
    	float totalTime;
    };

    struct SPUnityProfilerFrameInfo
    {
        float frameTime;
    	float playerTime;
        float openglTime;
        float presentTime;
        float physicsTime;
        float animationTime;
        float cullingTime;
        float skinningTime;
        float batchingTime;
        float renderTime;
        float scriptUpdateTime;
        float scriptFixedUpdateTime;
        float scriptCoroutinesTime;
        uint32_t drawCalls;
        uint32_t batchedDrawCalls;
        uint32_t tris;
        uint32_t batchedTris;
        uint32_t verts;
        uint32_t batchedVerts;
        uint32_t fixedUpdates;
    };

    EXPORT_API SPUnityProfilerFrameInfo SPUnityProfilerGetFrameInfo();
    EXPORT_API SPUnityProfilerGarbageInfo SPUnityProfilerGetGarbageInfo();
}

#endif